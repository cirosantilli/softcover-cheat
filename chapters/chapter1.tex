\chapter{Chapter 1}

\section{What PolyTeX maps to in HTML}

  \texttt{texttt}: \kode{span.tt}

  \textsc{textsc}: \kode{span.sc}

  \textbf{textbf}

  \textit{textit}

  \underline{underline}

  \LaTeX\ LaTeX: a bunch of carefully styled spans

  Quote:

  \begin{quote}
  begin quote
  \end{quote}

  \subsection{Links}

    \kode|href|: \href{http://example.com}{Example domain.}

    TODO: \kode{url}? Using the package only works for PDF output. %\href{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/zFrDngqlYbY}{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/zFrDngqlYbY}

    Markdown autolinks map to \kode{href} in PolyTeX.

    % \url{http://example.com}

  \subsection{Label}

    All labels becomes HTML IDs, and refs link to those IDs.

    \subsubsection{Label header}\label{label-header}

    \kode{ref} to this section~\ref{label-header}

      The tilde makes the descriptor \kode{section} become a single link with the \kode{ref}.

    \subsubsection{Cross file label}

      Label on another file: section~\ref{chapter-2}.

    \subsection{Header without label}

      Headers without labels cannot be referred to.

  \subsection{Image}

  \subsection{Figure}

    Figure~\ref{fig-simple} is a simple figure.

    \begin{figure}
      \image{images/image.png}
      \caption{Figure caption.}
      \label{fig-simple}
    \end{figure}

    Use the special command \kode{image} command to also center the image.

    Setting image size currently not possible but might be fixed: %\href{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/zldf2QceCGY}{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/zldf2QceCGY}

    TODO specify image without the \kode{images/} prefix? Like \kode{graphicspath}. %\href{https://groups.google.com/forum/?hl=en#!newtopic/softcover-publishing}{https://groups.google.com/forum/?hl=en#!newtopic/softcover-publishing}

    %\image{image.png}

  \subsection{Table}

    Table reference: Table~\ref{table}.

    \begin{table}
      \caption{Table caption}
      \label{table}
      \begin{tabular}{|r|lc|}
          \hline
          a & b & c \\
          \hline
          1 & 2 & 3 \\
          4 & 5 & 5 \\
          \hline
          \multicolumn{3}{|c|}{Multicolumn} \\
          \hline
        \end{tabular}
    \end{table}

    DRY table without columns specifiers through \kode{*}. TODO: not possible in HTML?

    %at Table~\ref{dry-table}.
    %
    %\begin{table}
    %  \caption{Table caption}
    %  \label{table}
    %  \begin{tabular}{*{9}{l}}
    %    a & b \\
    %    c & d \\
    %  \end{tabular}
    %\end{table}

  \subsection{Math}

    Display equation: Equation~\ref{equation}.

    \begin{equation}\label{equation}
      x^2
    \end{equation}

    Inline only with \kode{\(  \)}, not dollar \kode{\$}: \( x^2 \) \$x\^{}2\$.

  \subsection{Computer code listings}

    TODO: how to escape braces and backslash in kode? %\kode{\asdf{}. %\href{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/YLez1SIyDQ0}{https://groups.google.com/forum/?hl=en#!topic/softcover-publishing/YLez1SIyDQ0}

    Inline with the custom \kode{kode} macro.

    Display with the custom \kode{code} + \kode{codelisting} environments

    Code is aligned relative to the beginning of the line. TODO: align relative to begin as with \kode{lstautogobble}?

    Code reference: see listing~\ref{code}.

    Code language and options set through insane magic comment...

    \begin{codelisting}
      \label{code}
      \codecaption{Code caption.}
      %= lang:latex, options: "linenos": true
      \begin{code}
\kode{line 1}
\kode{line 2}
      \end{code}
    \end{codelisting}
